<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/DialerFragment">

    <fragment
        android:id="@+id/DialerFragment"
        android:name="cc.cans.canscloud.demoappinsdk.dialer.DialerFragment"
        android:label="Dialer Fragment"
        tools:layout="@layout/fragment_dialer">

        <action
            android:id="@+id/action_DialerFragment_to_CallFragment"
            app:destination="@id/CallFragment" />
    </fragment>

    <fragment
        android:id="@+id/CallFragment"
        android:name="cc.cans.canscloud.demoappinsdk.call.CallFragment"
        android:label="Call Fragment"
        tools:layout="@layout/fragment_call">
    </fragment>
</navigation>